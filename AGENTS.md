# AGENTS.md

## Overview
- Repository root for the education platform project.
- Prefer localized changes within the relevant module.

## Development
- Use Maven from the repo root (`pom.xml`).
- Keep changes consistent with existing style and patterns.

## Tests
- Run `mvn test` in the module you changed.
